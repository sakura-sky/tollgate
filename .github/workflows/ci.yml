# SPDX-License-Identifier: MIT
# SPDX-FileCopyrightText: 2026 Andrew Stevens

name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: fmt / clippy / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # Pin to the MSRV so CI fails if code uses anything newer than we promise.
      - name: Install Rust 1.85 (MSRV)
        run: |
          rustup toolchain install 1.85 --profile minimal --component rustfmt,clippy
          rustup default 1.85
          rustc --version

      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: Test
        run: cargo test --locked --all-targets

  reuse:
    name: REUSE compliance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: fsfe/reuse-action@v5

  audit:
    name: cargo audit (advisories)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Rust (stable, for the audit tool)
        run: rustup toolchain install stable --profile minimal
      - uses: Swatinem/rust-cache@v2
      # Use +stable explicitly: the repo's rust-toolchain.toml pins the MSRV in
      # this directory, but the audit tool itself should build on current stable.
      - name: Install cargo-audit
        run: cargo +stable install cargo-audit --locked
      - name: Audit dependencies
        run: cargo audit
